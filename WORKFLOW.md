# 闲鱼图文发布工作流程

本文档是本仓库唯一的发布流程说明。README 已删除；新增流程、规则或脚本时只更新本文件及对应测试。

## 1. 流程目标

把一个虚拟资料项目完整交付为：

1. 四张 `1080 × 1080` 商品图。
2. 一份不包含网盘链接的闲鱼正文。
3. 一份独立的夸克私密永久分享信息。
4. 一个由用户在最终确认后发布的闲鱼商品。

网盘分享和闲鱼发布是两个独立动作：

```text
源素材
  ↓
核验项目事实
  ↓
生成四张商品图
  ↓
生成并校验闲鱼正文
  ↓
制作独立夸克分享块
  ↓
Playwright 上传图片、填写表单
  ↓
发布前复核
  ↓
用户确认
  ↓
点击闲鱼发布并验证结果
  ↓
单独交付夸克链接和提取码
```

## 2. 开始前收集

每个项目开始时，一次性确认以下信息：

| 信息 | 用途 | 是否可以自动获取 |
| --- | --- | --- |
| 项目名称 | 标题、正文、分享标题 | 用户提供或从目录名确认 |
| 素材源目录 | 生成图片和统计文件 | 用户提供 |
| 文件数量和体积 | 正文事实信息 | 自动统计 |
| 代表图片 | 封面和详情图 | 从源目录选择 |
| 闲鱼价格 | 浏览器表单价格 | 用户提供 |
| 闲鱼分类 | 浏览器表单分类 | 页面识别或用户指定 |
| 发货方式 | 虚拟资料通常无需邮寄 | 默认“无需邮寄”，可由用户改 |

登录、短信验证码、APP 扫码和最终“发布”必须由用户本人完成。不要让用户重复提供已经存在于源目录、生成文件或网盘分享块中的信息。

## 3. 核验源素材

先统计源目录，不要直接开始生成：

```powershell
$root = "M:\WebDAV\夸克\软件\项目名"
Get-ChildItem -LiteralPath $root -File -Recurse |
    Measure-Object -Property Length -Sum
```

记录：

- 文件总数。
- 总大小。
- 主要文件格式。
- 可用于封面的 JPG/PNG。
- 是否包含可编辑源文件，如 CDR、AI、PSD、EPS、PDF、PPT。

没有核验过的文件数量、体积和格式不得写进商品正文。

## 4. 生成四张商品图

### 地图素材合集

```powershell
python render_map_collection.py `
  --root "M:\WebDAV\夸克\软件\高清一亿像素地图矢量图合集，超精细地理素材" `
  --out "D:\闲鱼\高清一亿像素地图矢量图合集，超精细地理素材"
```

脚本默认输出：

```text
01.png  封面与项目卖点
02.png  目录与分类
03.png  使用收获与适合场景
04.png  购买前说明与交付方式
```

### 其他项目

选择对应的 `render_*.py`：

```text
render_promo_music.py       宣传片背景音乐合集
render_camera_basics.py     相机基础课程
render_codex55.py           Codex 办公课程
render_workbuddy.py         WorkBuddy 课程
render_winrar_unified.py    WinRAR 资料
```

生成后必须检查：

```powershell
Get-ChildItem -LiteralPath "D:\闲鱼\项目名" -File |
    Where-Object { $_.Extension -eq ".png" } |
    Select-Object Name, Length
```

四张图必须都是 `1080 × 1080` PNG。还要目视确认文字未出框、图片未变形、封面和详情图风格一致。

## 5. 生成闲鱼正文

正文只描述商品本身和交付方式，不放分享链接或提取码。

正文结构：

```text
项目名称

一句话介绍

内容简介：
1. 内容分类
2. 文件格式
3. 文件数量和体积

适合人群和使用场景。

【说明】虚拟资料，拍后提供网盘链接。
```

### 地图项目正文示例

```text
高清一亿像素地图矢量图合集，超精细地理素材

一套高清地图与地理设计素材合集，包含中国地图、世界地图、各省及区域地图、专题地图和多种可编辑源文件。

内容简介：
1. 高清地图：中国地图、世界地图、各省高清图及区域地图。
2. 专题素材：交通、自然、旅游、区域等地图分类。
3. 源文件格式：CDR、AI、PSD、EPS、PDF、PPT等。
4. 资料规模：共468个文件，约7.24GB，按目录分类整理。

适合需要地图素材、矢量源文件和地理设计参考的用户。

【说明】虚拟资料，拍后提供网盘链接。
```

### 正文禁止内容

- `pan.quark.cn` 或其他网盘 URL。
- 分享 ID、提取码、密码。
- 价格、货币金额。
- 百度网盘、自动发货、实物、快递、物流等不准确表述。

## 6. 制作独立夸克分享块

使用夸克网盘官方工具搜索或创建项目文件夹的私密永久分享。分享块固定为：

```text
标题：项目名称
夸克私密永久分享链接：https://pan.quark.cn/s/分享ID
提取码：四位提取码
```

交付规则：

1. 标题、链接、提取码一次性整理。
2. 优先复制到剪贴板，必要时另存为本地临时文本。
3. 分享块可以发给买家或用户，但不能粘贴到闲鱼正文。
4. 不要把真实链接、提取码或网盘凭据提交到 Git。

`make_desc.py` 的 `write_project()` 会把 `quark_block` 拼进 `desc.txt` 和 `闲鱼发布文案_直接复制.txt`，这两个文件属于完整交付包，不能整段复制到闲鱼表单。实际填写时只复制商品正文部分，包含“拍后提供网盘链接”的说明，排除后面的分享块。

## 7. Playwright 发布

### 上传前

1. 打开闲鱼发布页。
2. 确认用户已登录。
3. 确认输出目录中有四张图。
4. 将四张图复制到 Playwright 允许访问的临时目录。

如果 Playwright 报 `File access denied`，只把图片复制到当前工作区内的临时目录后重新选择文件，不要修改图片内容。

### 填写表单

1. 点击“添加首图”。
2. 一次选择 `01.png`、`02.png`、`03.png`、`04.png`。
3. 确认四张图均已上传。
4. 选择页面允许的匹配分类；虚拟资料优先使用“电子资料”。
5. 填写正文。
6. 在价格字段填写用户指定价格，例如 `1` 元。
7. 选择“无需邮寄”（页面提供时）。
8. 不填写夸克链接、提取码或其他网盘信息。

### 状态记录

```text
已准备：图片、正文、分享块已生成
已上传：四张图已传入闲鱼页面，但还没有点击发布
待确认：表单已填好，等待用户最终确认
已发布：页面出现成功提示或商品详情链接
```

“已上传”不等于“已发布”，不能提前向用户声称商品已经公开。

## 8. 发布前复核

逐项核对当前页面：

```text
标题：项目名称，不含价格
分类：页面允许的匹配分类
图片：4 张
价格：用户指定价格
发货：无需邮寄（如适用）
正文：不含 pan.quark.cn、分享 ID、提取码
正文：不含百度、自动发货、实物、快递、物流
```

正文可先用本地命令检查：

```powershell
$copy = Get-Content -Raw "D:\闲鱼\项目名\闲鱼正文_直接复制.txt"
$copy | Select-String -Pattern "pan\.quark\.cn|提取码|分享ID|百度|自动发货|实物|快递|物流"
```

没有输出才表示没有命中这些禁止词。

## 9. 最终确认与发布

点击“发布”会代表用户向第三方平台公开发布商品。点击前必须向用户展示以下摘要，并等待明确确认：

```text
即将以当前闲鱼账号发布：
标题：项目名称
分类：电子资料
价格：1.00 元
图片：4 张
发货：无需邮寄
正文：不包含夸克链接和提取码
```

用户确认后：

1. 点击“发布”。
2. 等待成功提示、商品管理页或商品详情链接。
3. 记录商品链接或成功提示。
4. 再单独交付夸克链接和提取码。

## 10. 故障处理

| 问题 | 处理方式 |
| --- | --- |
| 源图片不存在 | 回到素材核验，不生成半成品 |
| 图片尺寸或格式错误 | 检查渲染脚本输出和 `validate_png_files()` |
| Playwright 拒绝路径 | 复制图片到工作区临时目录 |
| 页面要求扫码或验证码 | 暂停，请用户本人完成 |
| 分类提示网页版不支持 | 选择页面允许的分类，或说明需要 APP |
| 正文出现链接或提取码 | 清空正文并重新填写 |
| 用户未确认发布 | 停在“待确认”，不要点击发布 |
| 发布后没有成功提示 | 停在当前页面，报告实际状态，不声称成功 |

## 11. 代码验证和 Git

在提交代码前运行：

```powershell
python -m unittest discover -s tests -v
python -m py_compile cover_v2.py make_desc.py render_camera_basics.py render_codex55.py render_map_collection.py render_promo_music.py render_winrar_unified.py render_workbuddy.py xianyu_common.py
git diff --check
```

提交时只加入源代码、测试和本流程文件。不要提交：

- 夸克真实链接、提取码和网盘凭据。
- 浏览器 profile、Cookie、登录态和验证码。
- 交付图片、Playwright 临时上传目录、日志和缓存。

当前分支为 `xianyu`：

```powershell
git switch xianyu
git pull --rebase origin xianyu
git status --short
git add WORKFLOW.md
git commit -m "docs: rewrite publishing workflow"
git push origin xianyu
```
