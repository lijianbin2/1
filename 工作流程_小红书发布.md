# 小红书图文工作流程

适用技能目录：H:/Codex/.codex/skills/Auto-Redbook-Skills-main
沉淀自 MangoDisk / Dashi / Office 三次发布，必读。

## 一、标准流水线（每次发图文按顺序走）

1. 收料：用户给软件介绍 + 配图，存 work/（原图 PNG/JPG，原样保留）
2. 写稿：仿 work/Mango_xhs.md 写 Markdown（封面 + 若干正文卡片，separator 分页，professional 主题）
3. 渲染：见第二节命令，输出 outputs/cover.png + card_N.png
4. 校验：view_image 逐张看渲染图，查溢出错字，配图是否嵌入大图
5. 落盘：D:/小红书/项目名_日期/ 下放 图文/ 原图/ MD 发布文案.txt zip 说明.txt
6. 干跑：publish_xhs.py --dry-run 先验标题图片参数
7. 私发：默认私密，不加 public 参数，单次执行
8. 确认：查创作平台最新笔记是单篇新笔记，写 published 标记防重发
9. 公发：用户说公开发布/定时，才加 public 或 post-time 发

## 二、渲染与发布命令（workdir 必须是技能目录）

渲染：python -X utf8 scripts/render_xhs.py work/XXX.md -o outputs -t professional -m separator

发布模板：python -X utf8 scripts/publish_xhs.py -t 标题 -d 正文 -i cover.png card_1.png ...（1 到 15 张，cover 放第一张）

标题不超过 20 字；标题字数用 python len() 数，不要目测。
默认仅自己可见；公发才加 --public；定时加 --post-time，格式 YYYY-MM-DD HH:MM:SS。
话题标签直接写进正文，如 #MangoDisk #磁盘清理。
不要用 --topics 参数：话题接口返回缺 msg 字段会 KeyError 崩掉，表现为图已传完但笔记没提交。
MangoDisk 实测：带 --topics 必崩，去掉一次成功。

## 三、防重发铁律（Dashi 血案：并发调 post_note 重发 5 篇）

1. 发布前 tasklist 确认无 python.exe 残留发布进程。
2. publish 一次只调一次；post_note 无去重，调一次即一篇。
3. 成功后立刻写 outputs/published_XXX.txt（note_id + 时间），此后绝不重试该笔记。
4. 失败重发前先查创作平台最新笔记，确认没发出才重发。
5. 私发成功回执：note_id + explore 链接，提醒半小时后可公开，不主动公发。

## 四、查笔记脚本 H:/Codex/1/check_notes.py（绝对路径，从任何目录可跑）

python -X utf8 H:/Codex/1/check_notes.py 可列出最新 3 篇（标题 id 时间），用于确认发布结果、判断是否重发。
Cookie 在 .env 的 XHS_CREATOR_COOKIE；若提示登录失效，找用户要 creator.xiaohongshu.com 完整 Cookie 更新。

## 五、exec 执行层坑位

1. workdir 必须填真实存在目录，如技能目录或 C:/Users/1/Documents，否则报目录名称无效。
2. 命令文本里不出现美元符开头的词，会被当变量剥离；powershell 变量一律不用。
3. 超过 10 秒的任务会被转后台：发布命令一律加输出重定向到日志文件，再用 Get-Content 分段看日志轮询。
4. 中文标题正文路径用单引号包裹；路径用正斜杠；write_stdin 不可用，不要依赖它收后台输出。
5. 写中文文件用 python open 指定 utf-8，换行用 chr(10) 拼接，不用 heredoc。

## 六、用户偏好

1. 默认流程：私发，半小时后，公发。
2. 后续图文必须带用户配图，嵌入正文卡大图。
3. D:/小红书/项目文件夹 落盘全套文件：图文 原图 MD 发布文案 zip 说明。
4. 预览图用绝对路径展示。
