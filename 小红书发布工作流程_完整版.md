# 小红书图文工作流程

适用技能目录：H:/Codex/.codex/skills/Auto-Redbook-Skills-main
沉淀自 MangoDisk / Dashi / Office 三次发布，必读。

## 一、标准流水线（每次发图文按顺序走）

1. 收料：用户给软件介绍 + 配图，存 work/（原图 PNG/JPG，原样保留）
2. 写稿：仿 work/Mango_xhs.md 写 Markdown（封面 + 若干正文卡片，separator 分页，professional 主题）
3. 渲染：见第二节命令，输出 outputs/cover.png + card_N.png
4. 校验：view_image 逐张看渲染图，查溢出错字，配图是否嵌入大图
5. 落盘：D:/小红书/项目名_日期/ 下放 图文/ 原图/ MD 发布文案.txt zip 说明.txt
6. 干跑：publish_xhs.py --dry-run 先验标题图片参数
7. 私发：默认私密，不加 public 参数，单次执行
8. 确认：查创作平台最新笔记是单篇新笔记，写 published 标记防重发
9. 公发：用户说公开发布/定时，才加 public 或 post-time 发

## 二、渲染与发布命令（workdir 必须是技能目录）

渲染：python -X utf8 scripts/render_xhs.py work/XXX.md -o outputs -t professional -m separator

发布模板：python -X utf8 scripts/publish_xhs.py -t 标题 -d 正文 -i cover.png card_1.png ...（1 到 15 张，cover 放第一张）

标题不超过 20 字；标题字数用 python len() 数，不要目测。
默认仅自己可见；公发才加 --public；定时加 --post-time，格式 YYYY-MM-DD HH:MM:SS。
话题标签直接写进正文，如 #MangoDisk #磁盘清理。
不要用 --topics 参数：话题接口返回缺 msg 字段会 KeyError 崩掉，表现为图已传完但笔记没提交。
MangoDisk 实测：带 --topics 必崩，去掉一次成功。

## 三、防重发铁律（Dashi 血案：并发调 post_note 重发 5 篇）

1. 发布前 tasklist 确认无 python.exe 残留发布进程。
2. publish 一次只调一次；post_note 无去重，调一次即一篇。
3. 成功后立刻写 outputs/published_XXX.txt（note_id + 时间），此后绝不重试该笔记。
4. 失败重发前先查创作平台最新笔记，确认没发出才重发。
5. 私发成功回执：note_id + explore 链接，提醒半小时后可公开，不主动公发。

## 四、查笔记脚本 H:/Codex/1/check_notes.py（绝对路径，从任何目录可跑）

python -X utf8 H:/Codex/1/check_notes.py 可列出最新 3 篇（标题 id 时间），用于确认发布结果、判断是否重发。
Cookie 在 .env 的 XHS_CREATOR_COOKIE；若提示登录失效，找用户要 creator.xiaohongshu.com 完整 Cookie 更新。

## 五、exec 执行层坑位

1. workdir 必须填真实存在目录，如技能目录或 C:/Users/1/Documents，否则报目录名称无效。
2. 命令文本里不出现美元符开头的词，会被当变量剥离；powershell 变量一律不用。
3. 超过 10 秒的任务会被转后台：发布命令一律加输出重定向到日志文件，再用 Get-Content 分段看日志轮询。
4. 中文标题正文路径用单引号包裹；路径用正斜杠；write_stdin 不可用，不要依赖它收后台输出。
5. 写中文文件用 python open 指定 utf-8，换行用 chr(10) 拼接，不用 heredoc。

## 六、用户偏好

1. 默认流程：私发，半小时后，公发。
2. 后续图文必须带用户配图，嵌入正文卡大图。
3. D:/小红书/项目文件夹 落盘全套文件：图文 原图 MD 发布文案 zip 说明。
4. 预览图用绝对路径展示。

---

## 附录A：查笔记脚本完整代码
需要恢复 check_notes.py 时，从下面代码块原样另存即可，路径 H:/Codex/1/check_notes.py。
```python
import sys
sys.path.insert(0, 'H:/Codex/.codex/skills/Auto-Redbook-Skills-main/vendor/xhs_publish_runtime')
from dotenv import load_dotenv
load_dotenv('H:/Codex/.codex/skills/Auto-Redbook-Skills-main/.env')
import os
from apis.xhs_creator_apis import XHS_Creator_Apis
from xhs_utils.xhs_creator import XHSCreatorAuth
ck = os.getenv('XHS_CREATOR_COOKIE')
au = XHSCreatorAuth.from_cookie(ck)
api = XHS_Creator_Apis(au).bootstrap()
ok, msg, res = api.get_posted_notes_page(page=0, tab=0)
print('OK', ok, msg)
ns = res.get('data').get('notes')
print('count', len(ns))
for n in ns[:3]:
    print(n.get('display_title'), n.get('id'), n.get('time'))
au.close()
```

---

## 附录B：MangoDisk 实战记录

日期：2026-09-03 夜到 2026-09-04 凌晨
目标：MangoDisk 深度清理工具的小红书图文，私发先行，半小时后公发。

## 一、做完的事

1. 图文已就绪：C:/Users/1/Documents/Codex/2026-09-03/MangoDisk outputs/cover.png + card_1到7共8图，配图已嵌卡2大图，逐张校验无溢出。
2. D盘落盘：D:/小红书/MangoDisk_深度清理_20250903 图文 原图 MD 发布文案 zip 说明全套。
3. 私发成功：note_id 6a99a2bd00000000260229f4，仅自己可见，链接见第四节。
4. 工作流程沉淀为 H:/Codex/1/工作流程_小红书发布.md，查笔记脚本 H:/Codex/1/check_notes.py。
5. 技能目录垃圾已清，H:/Codex/1 已 git push 到 GitHub main（d834690）。

## 二、发布过程排障
1. publish_xhs.py 的 --topics 参数必崩：话题接口返回缺 msg 字段导致 KeyError，图传完但笔记不提交。改用话题标签写进正文一次成功。
2. 发布必须以技能目录为 workdir，否则缺 crypto-js：H:/Codex/.codex/skills/Auto-Redbook-Skills-main，里面有 node_modules 才能调 cookie 生成 x-s 等参数。
3. 发布命令超 10 秒会转后台：一律重定向到日志文件再 Get-Content 轮询；write_stdin 不可用，有会话 id 浮点解析 bug。
4. 防重发铁律：post_note 无去重，成功后绝不重试。Dashi 曾重发 5 篇一样的内容，用户手动删过。成功后写 outputs/published_mango.txt 标记。

## 三、文件位置清单
- 图文源：C:/Users/1/Documents/Codex/2026-09-03/MangoDisk/
- D 盘：D:/小红书/MangoDisk_深度清理_20250903/
- 流程文档：H:/Codex/1/工作流程_小红书发布.md
- 查笔记：python -X utf8 H:/Codex/1/check_notes.py
- 仓库：H:/Codex/1，GitHub lijianbin2/1.git，main 分支

## 四、笔记链接与待办公发
- 私发链接：https://www.xiaohongshu.com/explore/6a99a2bd00000000260229f4
- 状态：仅自己可见，创作平台已确认为最新单篇，2026-09-04 00:39，无重发。
- 待办：半小时后若用户说公开，需用 --public 重新发一篇公开版，私发篇改不了可见性，发前先查平台确认再发，避免重复。

## 五、用户偏好备忘
- 流程偏好：私发加半小时后加公发，图文必带配图，D 盘落盘，预览用绝对路径。
- exec 约束：workdir 填真实存在目录，命令禁 $ 开头词，中文用单引号包裹，路径用正斜杠。
合并说明：此单一文件含全部流程加实战记录，后续只维护这一个。
